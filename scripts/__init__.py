"""CLI entry points. Each script is runnable as `python scripts/<name>.py`.

Every script adds the repo root to sys.path (see `_path.py`) so imports like
`from core import load_config` resolve without needing `-m` or PYTHONPATH.
"""
