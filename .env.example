# SA key path — used by every script. The install.ps1 generates a real .env
# with an absolute path; this template is for manual setup.
GOOGLE_APPLICATION_CREDENTIALS=D:\LAB\vertex-ai-search\service-account.json

# GCP project id — must match config/config.yaml -> project.id
GOOGLE_CLOUD_PROJECT=commanding-way-380716
